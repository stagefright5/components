Density Sass functions and mixins have been extracted from `@material/density`. We do this to
avoid adding a new dependency to `@stagefright5/material` until we figured out how dependencies on
MDC are managed.
